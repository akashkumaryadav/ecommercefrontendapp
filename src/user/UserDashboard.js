import React from "react";
import Base from "../core/Base";

export const UserDashboard = () => {
  return (
    <Base title="User dashboard" descripton="this is user dasboard 😊">
      <h1>This is user dashboard</h1>
    </Base>
  );
};
